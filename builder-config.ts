import { Builder } from '@builder.io/react'

// Replace with your Builder.io API key
export const BUILDER_API_KEY = 'YOUR_API_KEY_HERE'

// This is the entry point for your Builder.io components
export const BUILDER_MODEL = 'page'

// Note: Builder.init() is now called in individual client components
// to avoid server-side rendering issues
